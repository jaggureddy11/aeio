{
  "name": "aeio-free-proxy",
  "version": "1.1.0",
  "description": "Hosted serverless proxy for Aeio Free tier with anonymous daily rate limiting",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "wrangler": "^3.80.0"
  }
}
