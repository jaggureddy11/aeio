import React from 'react';

export const MemoryPanel: React.FC = () => {
  return (
    <div className="memory-panel">
      {/* Memory panel skeleton */}
    </div>
  );
};

export default MemoryPanel;
